/**
 * Interaction Layer Injection — placeholder.
 *
 * `@xfcfam/xf-logger-file` contributes nothing to the Interaction Layer,
 * so its `A` declares no static slots. Kept structurally complete so the
 * artefact passes XF validation. NOT exported.
 */
export class A {
  private constructor() {}
  static async init(): Promise<void> {}
  static async terminate(): Promise<void> {}
}
