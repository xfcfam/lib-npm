# Link in-repo workspace packages that satisfy a dependency's semver range,
# so the explicit "@xfcfam/xf": "^0.1.0" peer deps resolve to the local
# package during development while published packages depend on the real
# @xfcfam/* on npm (never a workspace:/local reference).
link-workspace-packages=true
prefer-workspace-packages=true
